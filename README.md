# 🕌 GT-salat-dikr - نظام إشعارات الصلاة والأذكار

<div dir="rtl">

![Version](https://img.shields.io/badge/version-3.1.0-green.svg)
![License](https://img.shields.io/badge/license-GPL--2.0-blue.svg)
![Platform](https://img.shields.io/badge/platform-Linux-lightgrey.svg)
![Bash](https://img.shields.io/badge/bash-script-orange.svg)

**نظام متكامل لتذكير الصلاة والأذكار يعمل على Linux مع واجهة رسومية وSystem Tray**

---

## 📋 جدول المحتويات
- [✨ الميزات الجديدة](#الميزات-الجديدة)
- [🚀 التثبيت السريع](#التثبيت-السريع)
- [📦 التثبيت الكامل](#التثبيت-الكامل)
- [⚙️ إدارة البرنامج](#إدارة-البرنامج)
- [🔧 الأوامر المتاحة](#الأوامر-المتاحة)
- [🖥️ System Tray](#system-tray)
- [🔄 التحديث التلقائي](#التحديث-اللقائي)
- [🗑️ إلغاء التثبيت](#إلغاء-التثبيت)
- [❓ الأسئلة الشائعة](#الأسئلة-الشائعة)
- [🐛 استكشاف الأخطاء](#استكشاف-الأخطاء)
- [📄 الرخصة](#الرخصة)

---

## ✨ الميزات الجديدة في الإصدار 3.2.2-full

### 🚀 **التشغيل التلقائي الكامل**
- ✅ بدء تلقائي عند إقلاع النظام
- ✅ System Tray يظهر تلقائياً
- ✅ تأخير ذكي لتحميل واجهة المستخدم
- ✅ مراقبة وإعادة تشغيل تلقائي

### 🖥️ **System Tray المحسن**
- ✅ أيقونة في شريط المهام
- ✅ قائمة تحكم كاملة
- ✅ عرض مواقيت الصلاة
- ✅ تحديث تلقائي للمعلومات
- ✅ تنسيق نظيف بدون أكواد ANSI

### 💾 **تخزين محلي ذكي**
- ✅ عمل بدون اتصال بالإنترنت
- ✅ تخزين مواقيت 3 أشهر مسبقاً
- ✅ تحديث أسبوعي تلقائي
- ✅ استخدام ذاكرة تخزين مؤقت

### 🔔 **إشعارات متقدمة**
- ✅ إشعارات طرفية ونظامية
- ✅ تحكم منفصل في كل نوع إشعار
- ✅ تنبيه قبل الصلاة
- ✅ أذان كامل وقصير

### ⚙️ **إدارة سهلة**
- ✅ إعدادات تلقائية للموقع
- ✅ واجهة إعدادات تفاعلية
- ✅ سجلات تفصيلية
- ✅ تحديث ذاتي

---

## 🚀 التثبيت السريع

### الطريقة 1: تثبيت مباشر
```bash
# تحميل وتشغيل المثبت
curl -fsSL https://raw.githubusercontent.com/SalehGNUTUX/GT-salat-dikr/main/install-full.sh | bash
```

### الطريقة 2: تثبيت يدوي
```bash
# 1. تحميل الملفات
git clone https://github.com/SalehGNUTUX/GT-salat-dikr.git
cd GT-salat-dikr

# 2. تشغيل المثبت
chmod +x install-full.sh
./install-full.sh
```

### الطريقة 3: للمطورين
```bash
# تثبيت مع خيارات متقدمة
./install-full.sh --custom-location "24.7136" "46.6753" --method 4 --auto-update
```

---

## 📦 التثبيت الكامل

### المتطلبات الأساسية
```bash
# أدوات مطلوبة
- bash 5.0+
- curl
- jq
- notify-send (libnotify)
- python3 (لـ System Tray)
- pip (لـ pystray, pillow)
```

### خطوات التثبيت التفصيلية

#### 1. **فحص المتطلبات**
```bash
# سيقوم المثبت بفحص وتثبيت المتطلبات تلقائياً
# إذا كنت تريد التحقق يدوياً:
for cmd in curl jq notify-send python3; do
    command -v $cmd || echo "❌ $cmd غير مثبت"
done
```

#### 2. **تشغيل المثبت**
```bash
./install-full.sh
```

#### 3. **ماذا يحدث أثناء التثبيت؟**
```
✅ المرحلة 1: فحص المتطلبات
✅ المرحلة 2: تحميل البرنامج
✅ المرحلة 3: تحميل الأيقونات
✅ المرحلة 4: الكشف عن الموقع
✅ المرحلة 5: تطبيق الإعدادات
✅ المرحلة 6: تحميل مواقيت الصلاة
✅ المرحلة 7: إنشاء مدير التشغيل التلقائي
✅ المرحلة 8: إعداد التشغيل التلقائي
✅ المرحلة 9: إعدادات الطرفية
✅ المرحلة 10: تثبيت مكتبات Python
✅ المرحلة 11: التشغيل الاختباري
✅ المرحلة 12: العرض النهائي
```

#### 4. **التحقق من التثبيت**
```bash
# اختبار البرنامج
gtsalat --status

# رؤية System Tray
gtsalat --tray

# التحقق من التشغيل التلقائي
systemctl --user status gt-salat-dikr-autostart.service
```

---

## ⚙️ إدارة البرنامج

### التشغيل والإيقاف
```bash
# بدء الإشعارات
gtsalat --notify-start

# إيقاف الإشعارات
gtsalat --notify-stop

# إعادة التشغيل
gtsalat --notify-stop && sleep 2 && gtsalat --notify-start
```

### عرض المعلومات
```bash
# حالة البرنامج
gtsalat --status

# مواقيت اليوم
gtsalat --show-timetable

# الصلاة القادمة (عند فتح terminal جديد)
gtsalat
```

### التحديثات
```bash
# تحديث الأذكار
gtsalat --update-azkar

# تحديث مواقيت الصلاة
gtsalat --update-timetables

# تحديث البرنامج نفسه
gtsalat --self-update
```

---

## 🔧 الأوامر المتاحة

### 📊 المعلومات والعرض
```bash
gtsalat                    # عرض ذكر وموعد الصلاة
gtsalat --status          # عرض حالة البرنامج الكاملة
gtsalat --show-timetable  # عرض مواقيت الصلاة اليوم
gtsalat --logs            # عرض سجلات البرنامج
gtsalat --help            # عرض المساعدة
```

### ⚙️ الإعدادات
```bash
gtsalat --settings               # تعديل الإعدادات
gtsalat --change-notify-system   # تغيير نظام الخدمة
```

### 🔔 التحكم في الإشعارات
```bash
# الإشعارات العامة
gtsalat --enable-all-notify      # تفعيل جميع الإشعارات
gtsalat --disable-all-notify     # تعطيل جميع الإشعارات

# إشعارات الصلاة
gtsalat --enable-salat-notify    # تفعيل إشعارات الصلاة
gtsalat --disable-salat-notify   # تعطيل إشعارات الصلاة
gtsalat --enable-salat-terminal  # إشعارات الصلاة في الطرفية
gtsalat --enable-salat-gui       # إشعارات الصلاة في النظام

# إشعارات الذكر
gtsalat --enable-zikr-notify     # تفعيل إشعارات الذكر
gtsalat --disable-zikr-notify    # تعطيل إشعارات الذكر
gtsalat --enable-zikr-terminal   # إشعارات الذكر في الطرفية
gtsalat --enable-zikr-gui        # إشعارات الذكر في النظام
```

### 🧪 الاختبار
```bash
gtsalat --test-notify       # اختبار الإشعارات
gtsalat --test-adhan        # اختبار الأذان الكامل
gtsalat --test-adhan-short  # اختبار الأذان القصير
gtsalat --test-approaching  # اختبار تنبيه الاقتراب
```

### 🔄 التحديثات
```bash
gtsalat --update-azkar          # تحديث الأذكار
gtsalat --update-timetables     # تحديث مواقيت الصلاة
gtsalat --self-update           # تحديث البرنامج
gtsalat --enable-auto-update    # تفعيل التحديث التلقائي
gtsalat --disable-auto-update   # تعطيل التحديث التلقائي
gtsalat --auto-update-status    # حالة التحديث التلقائي
gtsalat --force-auto-update     # إجبار التحديث التلقائي
```

### 🖥️ System Tray
```bash
gtsalat --tray           # تشغيل System Tray
gtsalat --tray-restart   # إعادة تشغيل System Tray
gtsalat --tray-stop      # إيقاف System Tray
```

---

## 🖥️ System Tray

### الميزات
- **🕒 عرض الوقت الحالي**
- **🕌 الصلاة القادمة ووقتها**
- **📊 مواقيت اليوم الكاملة**
- **🔔 التحكم في الإشعارات**
- **⚙️ الإعدادات السريعة**
- **🔄 التحديث الفوري**

### الاستخدام
```
1. انقر على الأيقونة في شريط المهام
2. انقر بزر الماوس الأيمن للقائمة
3. اختر الخيار المطلوب
```

### خيارات القائمة
```
🕌 GT-salat-dikr
══════════════════
⏰ الصلاة القادمة: الظهر 13:25
📊 مواقيت اليوم
🕊️ إظهار ذكر
📈 حالة البرنامج
══════════════════
⚙️ الإعدادات
🔄 تحديث المواقيت
🔔 الإشعارات:
  ▶️ تشغيل
  ⏸️ إيقاف
🖥️ الأيقونة:
  🔄 إعادة تشغيل
  ❌ إغلاق
══════════════════
❓ المساعدة
```

---

## 🔄 التحديث التلقائي

### الميزات
```bash
# تفعيل التحديث التلقائي
gtsalat --enable-auto-update

# ستحدث هذه المهام تلقائياً:
1. 📅 تحديث مواقيت الصلاة أسبوعياً
2. 📥 تحميل بيانات 3 أشهر مسبقاً
3. 🔍 التحقق من تحديثات البرنامج
4. 📊 حفظ السجلات تلقائياً
```

### التحكم
```bash
# عرض الحالة
gtsalat --auto-update-status

# إجبار التحديث
gtsalat --force-auto-update

# التعطيل
gtsalat --disable-auto-update
```

### السجلات
```bash
# رؤية سجلات التحديث
tail -f ~/.GT-salat-dikr/startup.log

# رؤية آخر تحديث
cat ~/.GT-salat-dikr/.last_auto_update 2>/dev/null
```

---

## 🗑️ إلغاء التثبيت

### الطريقة الآمنة
```bash
# 1. الانتقال لمجلد البرنامج
cd ~/.GT-salat-dikr

# 2. تشغيل ملف الإزالة
chmod +x uninstall.sh
./uninstall.sh
```

### ماذا يحدث أثناء الإزالة؟
```
⚠️  التحقق والتأكيد
🛑 إيقاف الخدمات والعمليات
🗑️ إزالة ملفات التشغيل التلقائي
🔧 تنظيف إعدادات الطرفية
🗑️ إزالة مجلد التثبيت
🧹 تنظيف الملفات المؤقتة
🔍 التحقق النهائي
📊 التقرير النهائي
```

### خيارات الإزالة
```bash
# مع نسخة احتياطية (موصى به)
./uninstall.sh
# → سيحفظ إعداداتك وبيانات الصلاة

# بدون تأكيد
echo "y" | ./uninstall.sh

# إزالة قسرية
./uninstall.sh --force
```

### استعادة النسخة الاحتياطية
```bash
# إذا حفظت نسخة احتياطية أثناء الإزالة
cp -r ~/gt-salat-dikr-backup-*/settings.conf ~/.GT-salat-dikr/
./install-full.sh
```

---

## ❓ الأسئلة الشائعة

### Q1: لماذا لا تظهر أيقونة System Tray؟
**الحل:**
```bash
# 1. التحقق من مكتبات Python
python3 -c "import pystray, PIL"

# 2. تثبيت المكتبات
pip install --user pystray pillow

# 3. تشغيل يدوي
gtsalat --tray
```

### Q2: كيف أغير الموقع؟
```bash
# الطريقة 1: عبر الإعدادات
gtsalat --settings

# الطريقة 2: يدوياً
nano ~/.GT-salat-dikr/settings.conf
# عدل LAT و LON ثم أعد التشغيل
```

### Q3: البرنامج لا يبدأ تلقائياً؟
```bash
# التحقق من systemd
systemctl --user status gt-salat-dikr-autostart.service

# أو تشغيل يدوي
~/.GT-salat-dikr/autostart-manager.sh
```

### Q4: كيف أزيد وقت التنبيه قبل الصلاة؟
```bash
# في الإعدادات → اختر 30 دقيقة
# أو عدل الملف يدوياً
sed -i 's/PRE_PRAYER_NOTIFY=15/PRE_PRAYER_NOTIFY=30/' ~/.GT-salat-dikr/settings.conf
```

### Q5: كيف أعطل الإشعارات مؤقتاً؟
```bash
# إيقاف مؤقت
gtsalat --notify-stop

# استئناف
gtsalat --notify-start

# أو عبر System Tray: زر ⏸️
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: خطأ في bashrc/zshrc
```bash
# الإصلاح السريع
sed -i '/# GT-salat-dikr/,/fi/d' ~/.bashrc ~/.zshrc 2>/dev/null
source ~/.bashrc
```

### المشكلة: لا يوجد اتصال بالإنترنت
```bash
# سيعمل البرنامج بالبيانات المحفوظة
# لتحديث البيانات عند عودة الاتصال:
gtsalat --update-timetables
```

### المشكلة: System Tray لا يستجيب
```bash
# إعادة التشغيل
gtsalat --tray-stop
sleep 2
gtsalat --tray

# أو إعادة تشغيل المدير
systemctl --user restart gt-salat-dikr-autostart.service
```

### المشكلة: لا تظهر الإشعارات
```bash
# اختبار نظام الإشعارات
notify-send "اختبار" "هل ترى هذا الإشعار؟"

# إذا لم تظهر:
sudo apt install libnotify-bin  # لـ Debian/Ubuntu
```

### المشكلة: خطأ في DBUS
```bash
# إصلاح متغيرات البيئة
export DBUS_SESSION_BUS_ADDRESS="unix:path=/run/user/$(id -u)/bus"
export DISPLAY=":0"
```

### رؤية السجلات
```bash
# السجلات الرئيسية
tail -f ~/.GT-salat-dikr/notify.log

# سجلات التشغيل التلقائي
tail -f ~/.GT-salat-dikr/startup.log

# سجلات System Tray
python3 ~/.GT-salat-dikr/gt-tray.py  # في terminal منفصل
```

---

## 📄 الرخصة

هذا المشروع مرخص تحت رخصة MIT.

### حقوق النسخ والاستخدام
```
Copyright (c) 2025 SalehGNUTUX

يُسمح بالاستخدام، النسخ، التعديل، الدمج، النشر، التوزيع،
الترخيص من الباطن، و/أو بيع نسخ من البرنامج، شريطة أن
تتضمن جميع النسخ الإشعار الأصلي لحقوق الملكية.
```

### ضمانات
```
يُقدم البرنامج "كما هو"، دون أي ضمان من أي نوع، صريح أو ضمني،
بما في ذلك على سبيل المثال لا الحصر ضمانات القابلية للتسويق،
الملاءمة لغرض معين وعدم الانتهاك. في أي حال لا يكون المؤلفون
مسؤولين عن أي مطالبة، أضرار أو مسؤولية أخرى، سواء في عقد،
عمل غير مشروع أو خلاف ذلك، الناشئة عن أو في اتصال مع البرنامج
أو الاستخدام أو المعاملات الأخرى في البرنامج.
```

### المساهمة
```
1. Fork المشروع
2. أنشئ فرعاً لميزتك (git checkout -b feature/AmazingFeature)
3. أضف تغييراتك (git add .)
4. احفظ التغييرات (git commit -m 'Add some AmazingFeature')
5. ادفع للفرع (git push origin feature/AmazingFeature)
6. افتح Pull Request
```

---

## 👥 الاتصال والدعم

### الطرق المتاحة للمساعدة
- **📖 هذه الوثائق** - اقرأها بعناية
- **🐛 GitHub Issues** - للإبلاغ عن الأخطاء
- **💬 المناقشات** - للأسئلة والاقتراحات
- **🔄 تحديثات تلقائية** - للحصول على الإصلاحات

### قبل الإبلاغ عن خطأ
```bash
# جمع المعلومات
gtsalat --status
ls -la ~/.GT-salat-dikr/
tail -20 ~/.GT-salat-dikr/notify.log
```

### المساهمة في الترجمة
```
1. ترجمة ملفات azkar.txt
2. ترجمة رسائل البرنامج
3. إضافة دعم للغات جديدة
```

---

## 🌟 نصائح واستخدامات متقدمة

### 1. **دمج مع سكربتات أخرى**
```bash
# الحصول على موعد الصلاة القادمة في سكربت
NEXT_PRAYER=$(gtsalat --status | grep "الصلاة القادمة" | cut -d: -f2-)
echo "الصلاة القادمة: $NEXT_PRAYER"
```

### 2. **تخصيص الأذان**
```bash
# استبدال ملفات الأذان
cp /مسار/أذان/جديد.ogg ~/.GT-salat-dikr/adhan.ogg
cp /مسار/أذان/قصير.ogg ~/.GT-salat-dikr/short_adhan.ogg
```

### 3. **إضافة أذكار خاصة**
```bash
# إضافة إلى ملف الأذكار
echo "ذكر جديد%" >> ~/.GT-salat-dikr/azkar.txt
```

### 4. **جدولة مهام مع الصلاة**
```bash
# استخدام في cron مع وقت الصلاة
gtsalat --show-timetable | grep "العصر" | cut -d: -f2
```

### 5. **مراقبة استخدام البرنامج**
```bash
# رؤية الإحصائيات
grep "تم إرسال إشعار" ~/.GT-salat-dikr/notify.log | wc -l
```

---

## 🚀 بداية سريعة للمطورين

### هيكل المشروع
```
~/.GT-salat-dikr/
├── gt-salat-dikr.sh          # البرنامج الرئيسي
├── gt-tray.py               # System Tray
├── autostart-manager.sh     # مدير التشغيل التلقائي
├── settings.conf           # الإعدادات
├── azkar.txt              # الأذكار
├── icons/                 # الأيقونات
├── monthly_timetables/    # مواقيت الصلاة
├── notify.log            # سجلات الإشعارات
├── startup.log           # سجلات التشغيل
└── install.sh           # المثبت
```

### التطوير المحلي
```bash
# 1. Clone المشروع
git clone https://github.com/SalehGNUTUX/GT-salat-dikr.git
cd GT-salat-dikr

# 2. التعديل
nano gt-salat-dikr.sh

# 3. الاختبار
bash gt-salat-dikr.sh --status

# 4. التحديث
./install-full.sh
```

---

**✨ تم تطوير هذا المشروع بحب ❤️ لخدمة المجتمع الإسلامي**

**📧 للدعم والاستفسارات: راجع صفحة المشروع على GitHub**

**🕌 جعل الله هذا العمل في ميزان حسناتنا جميعاً**
